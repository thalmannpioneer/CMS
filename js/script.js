'use strict';

const add_toggle = () => {
    dark.classList.toggle("dark__visible");
    add.classList.toggle("add__visible");
};

const error_toggle = () => {
    error.classList.toggle("error__visible");
}